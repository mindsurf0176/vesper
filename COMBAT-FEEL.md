# 전투 감각 — 카운터사이드식 "격정" 처방

> 핵심: 격정은 **"좋은 걷기 애니/화려한 이펙트 한 개"가 아니라 시스템 3개**다.
> ① 위상 어긋남 동시다발 밀집 ② 임팩트 물리 가시화(넉백·플래시·히트스톱) ③ 강약 리듬(연출 등급제).
> **격정 = 절대량이 아니라 대비.** 평타는 연출0 베이스라인, 결정타만 "쾅".

## 우리가 정적인 이유 (1순위)
1. **타격이 HP만 깎음** — 맞는 쪽 반응(넉백/플래시/경직) 없음 → 손맛 0. "그냥 걸어다님"의 직접 원인.
2. **히트스톱·셰이크·사운드 동시발화 없음** → 타격이 허공 통과.
3. **연출 등급 차등 0** — 평타와 결정타 동일, 카메라 고정.
4. **발사 동기화** — 다 같이 쏘니 밀집 밀도 안 생김.

## 퀵윈 (에셋 0, 즉시) — 이번에 구현
- **히트스톱**: 명중 시 `Engine.time_scale` 떨궜다 복구(`create_timer(d, true, false, true)` = ignore_time_scale). 큰 타격/처치만.
- **히트플래시**: 피격 시 스프라이트 흰색 번쩍(modulate 강하게 + Glow로 블룸).
- **넉백**: 피격 유닛 시각 노드를 `-dir`로 밀었다 복귀(게임 좌표 X, 비주얼만).
- **발사 지터**: `atk_interval * randf_range(0.88,1.18)` + 스폰 시 초기 atk_timer 랜덤 → 동기화 깨서 밀도↑.
- **스크린쉐이크**: trauma 변수, `trauma^2`로 카메라 offset 노이즈, 감쇠. 타격 강도/처치별 차등.

## 풀 처방 (다음 단계)
- **Juice 싱글톤**: 모든 타격이 `spawn_hit_fx(pos,dir,tier)` 호출 → 플래시+넉백+히트스톱+셰이크+파티클+사운드 동시발화.
- **연출 등급제(fx_tier)**: 평타0 / 스페셜1(작은줌+짧은히트스톱) / 궁극기2(줌+슬로우+컷인+무적) / 처치3(타임스톱). 평타엔 절대 줌/쉐이크 X — 이 대비가 본질.
- **VFX 묶음**: 머즐플래시(additive)+탄피+트레이서(즉발히트도 0.05s "선")+착탄 스파크 동시발화. 근접=슬래시 호+X자 임팩트.
- **애니 위상**: 사격을 anticipation→impact(홀드 0.18s)→recovery로 프레임 타이밍 분할. 바쁜 idle(재장전).
- **동시다발 밀집**: 역할별 이질 애니 주기 + 발사 지터로 위상 어긋남.
- **전선 push-pull + 카메라 추적**: 라인이 출렁이고 카메라가 전선 무게중심 추적.
- **유닛 스케일업**: 카메라 줌으로 도트가 화면 1/5~1/4 차지(정수배 nearest). 마이크로 모션이 읽힘.
- **깊이 3레이어 + 림라이트 + Glow**: 패럴럭스 + 아군 시안/적 레드 1px 림.

## 정직한 한계
- 픽셀+1인은 Spine 2차 모션(망토 흔들림) 재현 불가 → 임팩트 프레임 홀드+히트스톱이 격차의 80% 메움.
- **타임스톱 중 게임로직(쿨/게이지) 멈춤 보장** 필요(time_scale 기반이면 process는 멈추나 실시간 타이머·셰이더 시간은 별도). 카운터사이드의 "정지 중 쿨 도는 버그" 답습 주의.
- **줌/쉐이크는 정수 픽셀 스냅** 안 하면 도트 뭉개짐.
- **사운드가 격정의 절반** — 임팩트 프레임과 1프레임 내 동기화된 저음 타격음 필수(에셋 병목).
- 연출 과포화 부작용(시인성/타임스톱 늘어짐) → 처음부터 등급제 + 토글 + 타임스톱 상한.
